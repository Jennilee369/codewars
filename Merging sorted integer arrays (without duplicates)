Write a function that merges two sorted arrays into a single one. The arrays only contain
integers. Also, the final outcome must be sorted and not have any duplicate.




answers:


function mergeArrays(a, b) {
  // your code here
let merge = a.concat(b)
  
let fusion = merge.sort((num1, num2) => num1 - num2)
  
  return fusion = [... new Set(fusion)]
}
